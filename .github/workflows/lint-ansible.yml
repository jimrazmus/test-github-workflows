name: Lint Ansible

on:
  pull_request:
    branches:
      - master

jobs:
  lint-ansible:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - uses: docker://cytopia/ansible-lint:latest
        with:
          args: -v ${{ github.workspace }}

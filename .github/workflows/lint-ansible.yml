name: Lint Ansible

on:
  pull_request:
    branches:
      - master

jobs:
  lint-ansible:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run the linter
        uses: docker://cytopia/ansible-lint:latest
        with:
          args: -vvv ${{ github.workspace }}
